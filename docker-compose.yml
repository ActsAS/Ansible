version: '3'
services:
  ansible5:
    container_name: 'ansible'
    image: 'ansible:5'
    build:
      context: '.'

    volumes:
      - './ansible:/srv/ansible:rw'

    hostname: 'ansible.container'
    networks:
      - 'vlan'
  
networks:
  vlan:
